console.log("BOM");

// setTimeout(() => {
//   location.reload(); // reload the website once after 5000ms
// }, 5000);

// so above code will reload the site in an interval of 5000 ms as each time the page refreshes code run again so do not use this method to reload

console.log(location.href);
// op: http://127.0.0.1:5501/JS/index.html

// we can change the href also location.href = 'any site link'
// then our site will not open instead it will be redirected
